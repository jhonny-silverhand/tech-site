/** Shared Gemini helper — now delegates to the central AI router.
 *  Kept for import compatibility (live-products, shopping route). */

export { AIError as GeminiError } from './ai-router';
export { aiJson as geminiJson } from './ai-router';
